<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create User</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #e9f7ef; /* Light green background */
            color: #2d6a4f; /* Dark green text */
            margin: 0;
            padding: 0;
        }
        /* Navbar styles */
        .navbar {
            background-color: #2d6a4f; /* Dark green */
            padding: 15px;
            text-align: center;
        }
        .navbar a {
            color: white;
            text-decoration: none;
            font-size: 18px;
            padding: 10px 20px;
        }
        .navbar a:hover {
            background-color: #81c784; /* Light green hover effect */
            border-radius: 5px;
        }
        .form-container {
            background: white;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            width: 300px;
            margin: 50px auto;
        }
        .form-container input, .form-container select {
            width: 100%;
            padding: 10px;
            margin: 10px 0;
            border: 1px solid #ccc;
            border-radius: 5px;
        }
        .form-container button {
            background-color: #81c784; /* Light green */
            color: white;
            padding: 10px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            width: 100%;
        }
        .form-container button:hover {
            background-color: #66bb6a; /* Darker green hover effect */
        }
    </style>
</head>
<body>
    <!-- Navbar -->
    <div class="navbar">
        <a href="/">Home</a>
    </div>

    <!-- Form Container -->
    <div class="form-container">
        <h2>Create User</h2>
        <form action="/create" method="POST">
            <input type="text" name="username" placeholder="Username" required>
            <input type="text" name="profileImage" placeholder="Profile Image URL" required>
            <input type="email" name="email" placeholder="Email" required>
            <input type="text" name="bio" placeholder="Bio">
            <select name="gender">
                <option value="male">Male</option>
                <option value="female">Female</option>
                <option value="other">Other</option>
            </select>
            <input type="number" name="age" placeholder="Age">
            <button type="submit">Create User</button>
        </form>
    </div>
</body>
</html>
